README.md clair expliquant comment lancer le projet avec Docker :

docker-compose up --build

Backend → http://localhost:8000

Frontend → http://localhost:3000

Admin Django → http://localhost:8000/admin
